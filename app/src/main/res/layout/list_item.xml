<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/default_dimen"
    android:paddingLeft="@dimen/default_dimen"
    android:paddingRight="@dimen/default_dimen"
    android:paddingBottom="@dimen/default_dimen">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:textSize="@dimen/default_text_size"
        tools:text="Siberian Shaman Takes Cross-Country Walk to Moscow to 'Expel Putin' From Power" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/default_dimen"
        android:paddingLeft="@dimen/default_dimen"
        android:paddingRight="@dimen/default_dimen"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/section"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="@dimen/sub_text_size"
            tools:text="Politics" />

        <TextView
            android:id="@+id/date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/sub_text_size"
            tools:text="Jul 5, 2019 - 8:25 UTC" />

    </LinearLayout>
</LinearLayout>